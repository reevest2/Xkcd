@model List<xkcd.Models.Xkcd>



@{
    ViewData["Title"] = "Comics";
}
<!-- 
    <div class="d-flex justify-content-center">
    <table class="table">
        <tbody>
            @foreach (var xkcd in Model)
            {
                <tr>
                    <td>@xkcd.Num</td>
                    <td>@xkcd.Title</td>
                    <td>@xkcd.Link</td>
                </tr>
            }
        </tbody>
    </table>
</div> 
    
 -->
<div class="container">

</div>

<!--This is where the comic image is displayed-->
<div class="container">
    @foreach (var xkcd in Model)
     {
        <div class="row justify-content-center">
            <div class="col-md-8 offset-md-4">
                <p>Comic Number: @xkcd.Num</p>
            </div>
        </div>

         <div class="row">
             <div class="col-md-8 offset-md-4">
                <img src="@xkcd.Img">
            </div>
         </div>
     }
</div>

